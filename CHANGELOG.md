# Changelog

All notable changes to the **Classroom Platform** project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

---

## [Unreleased]

### Added
- Complete project repository blueprint and architectural directory layout.
- High-level system architecture and domain models spanning 19 core domains.
- Full documentation suite under `docs/` covering requirements, architecture, API specs, database models, engineering rules, and deployment guides.
- Architectural Decision Records (ADRs 001 through 005) covering proposed technology stack, authentication, file storage, real-time messaging, and live streaming.
- Monorepo folder blueprints for web (`apps/web`), mobile (`apps/mobile`), backend (`backend/api`), shared packages (`packages/`), infrastructure (`infrastructure/`), scripts (`scripts/`), and tests (`tests/`).
- Standard GitHub issue and pull request templates.
